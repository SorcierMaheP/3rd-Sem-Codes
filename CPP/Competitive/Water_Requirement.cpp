#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T, N;
    scanf("%d", &T);
    for (int i = 1; i <= T; i++)
    {
        scanf("%d", &N);
        printf("%d\n", 2 * N);
    }
    return 0;
}